// Input ports (use case interfaces)
// 
// Currently, use cases are defined in application/use_cases/commands/
// This directory is reserved for future explicit input port traits
// that would be implemented by the application layer.
//
// For now, the command structs in application layer serve as implicit input ports.
